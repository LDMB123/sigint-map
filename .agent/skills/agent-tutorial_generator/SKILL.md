---
name: $(basename "$yaml_file" .yaml)
description: Agent definition from .claude/agents
---

# Agent: $(basename "$yaml_file" .yaml)

This is an agent definition file. See `agent.yaml` for the full specification.
