# Vertex AI (Gemini 3 Pro Image) config
# Lines in KEY=VALUE format will be loaded by scripts/nanobanana-direct.js

# Project + location
GOOGLE_CLOUD_PROJECT=gen-lang-client-0925343693
VERTEX_LOCATION=global

# Auth (choose one):
# 1) Service account JSON (recommended for servers)
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json

# 2) gcloud ADC (local dev)
# Run: gcloud auth application-default login
