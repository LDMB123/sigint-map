# JSDoc Extraction Summary

The project now keeps type information inline in JSDoc within the runtime
JavaScript modules. No separate declaration files are generated.

If you need to adjust types, update the JSDoc annotations in the relevant
`.js` sources.
