# Server-side upstream credentials for production data sources.
# These are intentionally not shipped to the browser bundle.

FIRMS_API_KEY=
OPENRADIATION_API_KEY=

# Public site URL — used for sitemap and robots.txt.
# Defaults to the Vercel deploy URL if unset.
NEXT_PUBLIC_SITE_URL=
