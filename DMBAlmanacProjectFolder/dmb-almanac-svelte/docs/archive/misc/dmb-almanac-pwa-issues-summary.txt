==============================================================================
DMB ALMANAC PWA DEBUGGING - ISSUES SUMMARY
==============================================================================

ANALYSIS COMPLETED: 2026-01-20
TARGET: Chromium 143 / Apple Silicon / Next.js 16

==============================================================================
CRITICAL ISSUES (Fix Immediately)
==============================================================================

1. MISSING FUNCTION IN SERVICE WORKER
   Severity: CRITICAL
   File: /public/sw.js (Line 90)
   Issue: Function cleanupExpiredCaches() is called but never defined
   Impact: ReferenceError will be thrown on SW activation
   Fix Time: 15 minutes

   Affected Code:
   Line 89-91:
     .then(() => {
       return cleanupExpiredCaches(); // UNDEFINED!
     })

   Solution: Add cleanupExpiredCaches() function implementation
   See: dmb-almanac-pwa-fixes.md (FIX #1)

---

2. SERVICE WORKER NOT ACTIVATING IMMEDIATELY
   Severity: CRITICAL
   File: /public/sw.js (Lines 46-62)
   Issue: skipWaiting() not called on install, delays SW activation
   Impact: Users see update available but new SW doesn't take over quickly
   Fix Time: 5 minutes

   Current:
     Line 57-59: // NOTE: "We don't auto-skip here to allow controlled updates"
     (But no skipWaiting() is called anywhere!)

   Solution: Add self.skipWaiting() after precache completes
   See: dmb-almanac-pwa-fixes.md (FIX #2)

---

3. MEMORY LEAK IN OFFLINE DOWNLOAD
   Severity: CRITICAL
   File: /lib/storage/offline-download.ts (Line 375)
   Issue: Event listener not removed on timeout, accumulates with each download
   Impact: Memory leak, stale event listeners, multiple notifications on retry
   Fix Time: 20 minutes

   Current:
     Line 322: navigator.serviceWorker.addEventListener("message", messageHandler);
     Line 375: Removed only in cancellation path, NOT in timeout path

   Solution: Add removeEventListener in timeout handler + finally block
   See: dmb-almanac-pwa-fixes.md (FIX #3)

---

4. INDEXEDDB NOT AUTO-INITIALIZED
   Severity: CRITICAL
   File: /lib/storage/offline-db.ts (Line 264)
   Issue: Database created but never opened on app startup
   Impact: Offline data not available, app thinks database is empty
   Fix Time: 30 minutes

   Current:
     Line 264: export const offlineDb = new DMBOfflineDB();
     (Created but not opened)

   Solution:
     a) Create OfflineDataProvider that opens DB on mount
     b) Wire into app layout
     c) Call data-loader on first init
   See: dmb-almanac-pwa-fixes.md (FIX #4)

---

HIGH PRIORITY ISSUES
==============================================================================

5. iOS PWA DETECTION SUBOPTIMAL
   Severity: HIGH
   File: /lib/sw/register.ts (Lines 222-239)
   Issue: Detection order and type coercion problems
   Impact: iOS installed detection may fail, false positives possible
   Fix Time: 5 minutes

   Problems:
     - Checks media query before iOS-specific property
     - Type coercion: (navigator as unknown as { standalone?: boolean })
     - Query param fallback too weak

   Solution: Reorder checks (iOS first) and clean up type casting
   See: dmb-almanac-pwa-fixes.md (FIX #5)

---

6. NETWORK TIMEOUT TOO SHORT
   Severity: HIGH (Medium on fast networks)
   File: /lib/sw/serwist.config.ts (Line 143)
   Issue: 3 second timeout for API routes insufficient for slow networks
   Impact: Premature cache fallback on 4G/slow mobile, worse UX
   Fix Time: 10 minutes

   Current:
     Line 143: networkTimeoutSeconds: 3, (Too aggressive!)
     Line 178: networkTimeoutSeconds: 5, (Borderline)

   Solution: Increase to 5-8 seconds based on route type
   See: dmb-almanac-pwa-fixes.md (FIX #6)

---

7. DOWNLOAD POLLING TOO FREQUENT
   Severity: MEDIUM (High battery impact)
   File: /lib/storage/offline-download.ts (Line 369)
   Issue: 500ms polling = 120 DB queries/minute per download
   Impact: High CPU usage, battery drain on mobile, IndexedDB lock contention
   Fix Time: 10 minutes

   Current:
     Line 369: }, 500); // Polling every 500ms

   Solution: Increase to 2000ms (still responsive, much lower resource use)
   See: dmb-almanac-pwa-fixes.md (FIX #7)

---

MEDIUM PRIORITY ISSUES
==============================================================================

8. DATA LOADER NOT WIRED TO APP
   Severity: MEDIUM
   File: /lib/db/dexie/data-loader.ts
   Issue: Data loader module exists but not called anywhere
   Impact: IndexedDB remains empty on first install, offline features don't work
   Status: Depends on FIX #4 (OfflineDataProvider)

   Solution: Call from OfflineDataProvider.useEffect() on mount
   See: dmb-almanac-pwa-fixes.md (FIX #4)

---

9. OFFLINE PAGE SHOWS NO CACHED CONTENT
   Severity: MEDIUM
   File: /app/offline/page.tsx (Lines 5-62)
   Issue: Offline fallback page is static, doesn't show cached data
   Impact: User goes offline and can't see recently viewed content
   Status: Needs integration with IndexedDB to display cached data

   Solution:
     Add components showing:
     - Recent cached shows
     - Recently viewed songs
     - Downloaded tours/venues

   Not addressed in fixes (UX enhancement)

---

10. INSTALL PROMPT TIMING LOGIC COMPLEX
    Severity: LOW
    File: /components/pwa/InstallPromptBanner.tsx (Lines 41-62)
    Issue: Two overlapping useEffect hooks managing similar state
    Impact: Possible double-show or confusing timing

    Solution: Merge into single state machine effect
    Priority: After critical fixes

---

INFORMATION ITEMS (No Action Needed)
==============================================================================

11. WEB APP MANIFEST - EXCELLENT
    Status: NO ISSUES
    File: /public/manifest.json
    Comments:
      - All required fields present
      - Icons properly configured (192x192, 512x512, maskable)
      - Screenshots configured (desktop + mobile)
      - Shortcuts configured
      - Cache headers properly set in next.config.ts

    No action needed.

---

12. INSTALLATION FLOW - GOOD
    Status: MOSTLY GOOD, minor improvements possible
    File: /components/pwa/InstallPrompt.tsx

    Good:
      - beforeinstallprompt event captured correctly
      - appinstalled event handler present
      - Display mode media query monitored
      - User choice tracked

    Minor improvements:
      - Add try-catch around prompt() call
      - Add error messages if prompt fails
      - Log specific errors for debugging

---

13. DEXIE.JS INTEGRATION - EXCELLENT
    Status: VERY GOOD ARCHITECTURE
    Files: /lib/db/dexie/db.ts, /lib/storage/offline-db.ts

    Good:
      - Proper singleton pattern with lazy init
      - Comprehensive schema versioning (v1, v2, v3)
      - Compound indexes for common queries
      - Automatic timestamp hooks
      - Transaction support
      - Storage quota checking

    Gap: Not automatically initialized (depends on FIX #4)

---

==============================================================================
FILE CHANGES REQUIRED
==============================================================================

Files Requiring Changes:
  1. /public/sw.js                              - FIX #1, #2
  2. /lib/storage/offline-download.ts           - FIX #3, #7
  3. /lib/sw/register.ts                        - FIX #5
  4. /lib/sw/serwist.config.ts                  - FIX #6
  5. /components/pwa/OfflineDataProvider.tsx    - NEW FILE (FIX #4)
  6. /app/layout.tsx                            - IMPORT (FIX #4)

Files NOT Requiring Changes (Already Good):
  - /public/manifest.json
  - /lib/db/dexie/db.ts
  - /lib/db/dexie/offline-db.ts
  - /components/pwa/InstallPrompt.tsx
  - /components/pwa/ServiceWorkerProvider.tsx
  - /lib/sw/register.ts (good except iOS detection)

---

==============================================================================
DEPLOYMENT TIMELINE
==============================================================================

Phase 1: CRITICAL FIXES (Deploy Same Day)
  Duration: 45 minutes development
  Fixes: #1, #2, #3
  Testing: Offline SW lifecycle, cache cleanup, no error on activation
  Deployment: Staging → Production
  Rollback Plan: Revert to previous SW version if error rate increases

  Checklist:
    [ ] Fix #1: cleanupExpiredCaches() implemented
    [ ] Fix #2: skipWaiting() called after precache
    [ ] Fix #3: Event listener cleanup in finally block
    [ ] Tested in Chrome DevTools (offline mode)
    [ ] No console errors on SW activation
    [ ] Deployed to production

---

Phase 2: DATA INITIALIZATION (Deploy Next Day)
  Duration: 30 minutes development
  Fixes: #4
  Testing: IndexedDB opens on app mount, initial data loads
  Deployment: Staging → Production

  Checklist:
    [ ] OfflineDataProvider created and wired
    [ ] IndexedDB opens on first app load
    [ ] Data loader called on initialization
    [ ] Test on real device (mobile + desktop)
    [ ] No errors in offline storage initialization

---

Phase 3: PERFORMANCE & POLISH (Deploy Same Week)
  Duration: 30 minutes development
  Fixes: #5, #6, #7
  Testing: Network timeout behavior, polling efficiency, iOS detection
  Deployment: Staging → Production

  Checklist:
    [ ] Fix #5: iOS detection works on iOS Safari
    [ ] Fix #6: Network timeout increased, API requests work on slow networks
    [ ] Fix #7: Download polling at 2s interval, CPU usage reduced
    [ ] Performance testing on 4G throttle
    [ ] Battery usage testing on mobile

---

==============================================================================
TESTING CHECKLIST
==============================================================================

Before Each Deployment:
  [ ] npm run lint - No errors
  [ ] npm run build - Successful build
  [ ] npm start - App launches without errors

  DevTools Tests:
    [ ] Application > Service Workers: Active and running
    [ ] Application > Cache Storage: Expected caches present
    [ ] Application > IndexedDB: dmb-almanac-offline open with tables
    [ ] Network tab: Offline simulation works
    [ ] Console: No errors or warnings

  Offline Tests (Simulate with DevTools > Network > Offline):
    [ ] Navigate to /songs - loads from cache
    [ ] Navigate to /venues - loads from cache
    [ ] Search functionality works with cached data
    [ ] Shows cached offline page on navigation failure
    [ ] No 404 errors for cached resources

  Update Tests:
    [ ] Deploy new SW version
    [ ] User sees "Update available" notification
    [ ] Click update triggers new SW activation
    [ ] New version loads without page reload lag
    [ ] No "waiting" state in DevTools

  Installation Tests:
    [ ] Desktop Chrome: Install banner shows after scroll
    [ ] Install succeeds and app launches from home screen
    [ ] iOS Safari: Manual "Add to Home Screen" works
    [ ] App launches in fullscreen mode (standalone)
    [ ] Offline functionality works in standalone mode

  Performance Tests:
    [ ] Network throttle (Slow 4G): API requests don't timeout prematurely
    [ ] Download large file (100+ pages): CPU usage monitored
    [ ] Battery usage on iOS: No excessive polling
    [ ] Memory profiler: No memory leaks in event listeners

---

==============================================================================
DOCUMENTATION UPDATES NEEDED
==============================================================================

Files to Update:
  1. /lib/sw/PWA_CACHE_MANAGEMENT.md
     - Update to reflect actual implementation
     - Document new cleanupExpiredCaches() function
     - Add troubleshooting section

  2. /lib/db/dexie/DATA_LOADER.md
     - Explain how data-loader is called from OfflineDataProvider
     - Add initialization sequence diagram

  3. Create: /docs/PWA_DEPLOYMENT.md
     - Document deployment checklist
     - Include testing procedures
     - Add troubleshooting guide

  4. Create: /docs/OFFLINE_ARCHITECTURE.md
     - SW lifecycle explanation
     - Dexie.js schema documentation
     - Data sync flow

---

==============================================================================
QUESTIONS FOR TEAM
==============================================================================

1. Should cache cleanup run periodically (every 10 minutes) or only on activation?
   Current implementation: Only on activation
   Recommendation: Add periodic cleanup via message from app

2. What's the target storage quota warning threshold?
   Current: No warning
   Recommendation: Warn at 80% of available quota

3. Should iOS PWA limitations be documented in the app UI?
   Current: Not documented
   Recommendation: Show info banner on iOS with limitations

4. Do you want automatic data loader on app startup or manual trigger?
   Current: Manual (not called)
   Recommendation: Automatic with progress indicator

5. Should downloads resume after browser restart?
   Current: No resume capability
   Recommendation: Implement resume for partial downloads

---

==============================================================================
PERFORMANCE TARGETS
==============================================================================

After All Fixes:

LCP (Largest Contentful Paint): < 1.0s
  Current state: Likely 0.8-1.2s (depends on cache hit)
  After fixes: 0.6-0.9s (SW caching active immediately)

INP (Interaction to Next Paint): < 100ms
  Current state: Likely 150-200ms (depends on data loading)
  After fixes: 80-150ms (improved with 2s polling interval)

CLS (Cumulative Layout Shift): < 0.05
  Current state: Good (0.02-0.04)
  After fixes: Unchanged (no changes to layout)

FCP (First Contentful Paint): < 1.0s
  Current state: Likely 1.0-1.5s
  After fixes: 0.8-1.1s (SW caching active)

TTFB (Time to First Byte): < 400ms
  Current state: Depends on server
  After fixes: Unchanged

---

==============================================================================
SEVERITY RATINGS EXPLAINED
==============================================================================

CRITICAL (Fix Immediately)
  - Blocks core PWA functionality
  - Will cause user-facing errors
  - May crash service worker
  - Prevents offline usage

HIGH (Fix Within 24 Hours)
  - Impacts user experience significantly
  - May cause degraded performance
  - Affects battery/CPU on mobile
  - Prevents feature from working properly

MEDIUM (Fix Within 1 Week)
  - Nice-to-have improvements
  - Minor performance issues
  - Affects specific use cases
  - Can be scheduled with other work

LOW (Fix When Convenient)
  - Code quality issues
  - Minor UX improvements
  - Not user-blocking
  - Good-to-have optimizations

---

END OF REPORT
==============================================================================
Generated: 2026-01-20
For questions or clarifications, see:
  - dmb-almanac-pwa-debug-report.md (full analysis)
  - dmb-almanac-pwa-fixes.md (detailed fixes with code)

