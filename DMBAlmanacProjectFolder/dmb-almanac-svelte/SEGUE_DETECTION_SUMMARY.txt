================================================================================
SEGUE DETECTION FIX - QUICK REFERENCE
================================================================================

FILE MODIFIED:
  /Users/louisherman/ClaudeCodeProjects/DMBAlmanacProjectFolder/dmb-almanac-svelte/scraper/src/scrapers/shows.ts

PROBLEM:
  - Only 4 segues detected out of ~50,000 setlist entries (0.008%)
  - DMB is known for having thousands of segues

ROOT CAUSE:
  - Detection code was looking for visual characters (→, >, ->) in notes text
  - Actual segue marker is the » character (U+00BB) in <span class="setitem"> element
  - Wrong HTML element being analyzed

SOLUTION:
  Changed from checking notes text to checking setitem span for » character

CODE CHANGES SUMMARY:
  Line 232-245: Replace isSegue detection logic
    OLD: const isSegue = notesText.includes("→") || ...
    NEW: const setitemSpan = $row.find("span.setitem");
         const isSegue = setitemContent.includes("»") || ...

  Line 268-277: Add post-processing for segueIntoTitle population
    NEW: Loop through setlist and populate segueIntoTitle from next song

IMPROVEMENT:
  Before: 4 segues
  After:  6,572 segues
  Increase: +164,300%

VERIFICATION:
  Test script: test-segue-all-shows.ts
  Result: 13.18% of setlist entries are segues (expected 10-15% for DMB)

FILES CREATED (DOCUMENTATION):
  - SEGUE_DETECTION_FIX.md (detailed explanation)
  - SEGUE_FIX_ANALYSIS.md (technical analysis)
  - test-segue-detection.ts (single show test)
  - test-segue-all-shows.ts (comprehensive test)

EXAMPLE SEGUES NOW DETECTED:
  - Recently → Digging a Ditch
  - So Much to Say → Anyone Seen the Bridge
  - Big Eyed Fish → Bartender
  - Sleep to Dream Her → Grace Is Gone
  - Everyday → All Along the Watchtower

BACKWARD COMPATIBILITY:
  ✓ No schema changes
  ✓ No breaking changes
  ✓ Uses existing ScrapedSetlistEntry fields
  ✓ Fully compatible with existing code

TESTING:
  Run: cd scraper && npx tsx test-segue-all-shows.ts
  Result: 6,572 segues in 2,259 shows (59.9% of all shows have segues)

NEXT STEPS:
  1. Deploy updated shows.ts
  2. Re-run scraper on all shows
  3. Verify segue data in database
  4. Update show JSON exports with new segue data

================================================================================
